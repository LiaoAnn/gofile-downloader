class Chunk {
    static totalSize = 0;
    static fileName = "";
    static subChunkSize = 1024 * 1024 * 4; //8MB

    constructor(options) {
        const {url, chunkSize, chunkIndex}
    }
}

module.exports = Chunk;